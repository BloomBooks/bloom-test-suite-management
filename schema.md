# Test Import Schema

## Purpose

This document is the import handoff reference.

It is written for someone starting cold who needs to understand:

- what the generated JSON files are
- how they are produced from the CSV
- how those fields map into the single live Notion database
- which parts of the workflow are preparation versus transport

If this file and the code disagree, the code is authoritative. In practice, the pipeline is intentionally split so that:

- `prepare-import.mjs` interprets and normalizes spreadsheet content
- `import-to-notion.mjs` mostly transports prepared values into Notion

## Model Change (current plan)

After testing, the model was simplified to a **single Notion database: `Test Case Runs`.**

- There is **no** separate `Test Cases` database. The full test case *definition* is folded onto every run card: durable metadata as its own properties, and the test steps / notes as the run-card page body. A run card is the merge of the case definition and the specifics of one run.
- There is **no** separate `Test Suite Runs` database. Suite-run membership is recorded as a closed `Test Suite Run` **select tag** on each run card, not as a relation.

The old `Test Cases` and `Test Suite Runs` databases that may still exist in the workspace are intentionally left untouched. The importer simply stops writing to them, and they are dropped from `notion-config.json`.

### Clean slate on the Notion side

The import now starts from a clean slate under a new root page, `Bloom-Tests` (`37d4bb19df128097a7f9f7f0ab9f1a2f`). `import-to-notion.mjs` creates a fresh `Test Case Runs` database inside that page on first run:

- If `notion-config.json` has a non-empty `databases.testCaseRuns`, that id is used.
- Otherwise, if `notion-state.json` already records a created `databaseId`, that one is reused.
- Otherwise the importer creates a new `Test Case Runs` database under `parentPageId`, writes its id into `notion-state.json` as `databaseId`, and prints it.

A freshly created database already has the full property schema, so live-schema reconciliation is skipped on the creating run.

## Files And Roles

### Inputs

- `Bloom Test Plan.csv`
  The source spreadsheet.

- `notion-config.json`
  Stores `parentPageId` (the `Bloom-Tests` root page) and `databases.testCaseRuns`. Leave `testCaseRuns` empty (`""`) to have the importer create a fresh database under `parentPageId`.

- `area-mapping.json`, `title-mapping.json`, `step-overrides.json`
  Curation inputs read by `prepare-import.mjs` to interpret the spreadsheet: `area-mapping.json` classifies context rows (area / instruction / ignore) and supplies area names and prepended instructions; `title-mapping.json` and built-in title overrides produce clean case titles; `step-overrides.json` replaces the auto-derived checklist steps / notes for specific cases by `Import ID`.

### Generated By `prepare-import.mjs`

The default output folder is `output/`.

- `test-case-runs.json`
  The only Notion-bound file. One record per `(case, suite run)` pair that has execution data, with durable case metadata folded in.

- `suite-run-tags.json`
  The distinct, ordered list of `Test Suite Run` tag values discovered in the spreadsheet. This documents the closed select list; the importer also creates options on demand.

- `prepare-summary.json`
  Counts and metadata about the preparation pass.

- `date-warnings.json`
  Rows whose run-date values could not be normalized cleanly.

### Generated By `import-to-notion.mjs`

- `notion-state.json`
  Import state keyed by `importRunId`. This is how the importer knows whether to create or update a run card.

- `notion-import-failures.json`
  Append-only failure log for case runs.

The importer also writes body content into Notion run pages and can reconcile the live `Test Case Runs` schema.

## End-To-End Flow

1. Parse the spreadsheet.
2. Detect run-column slots (one per suite run / platform).
3. For each importable row, derive one logical test case (kept in memory only).
4. Derive one `Test Case Run` per `(case, suite run)` pair that has execution data, folding the case metadata onto it.
5. Collect the distinct suite-run names as the closed `Test Suite Run` tag list.
6. Upload the prepared run records into the single Notion database.

The main design rule is:

- semantic interpretation happens in preparation
- Notion upload should not need to rediscover meaning from raw prose

## Core Entity

The model now has one persisted entity.

### `Test Case Run`

One instance of a test case in one suite run. This is the actionable work card testers use in Notion, and it is the only Notion database.

Each run card carries:

- the run-specific execution data (assignee, date, build, issues, OK)
- the suite-run identity as a `Test Suite Run` select tag
- the durable case metadata (title summary, legacy number, dokimion id, priority, past issues, estimated time, functional areas, step description, original description) as its own properties
- the full test case definition — the parsed checklist steps and notes — as a checkable to-do list in the page body, plus the imported execution details

The "test case" and "test suite run" still exist as concepts during preparation, but they are not separate databases. A test case is simply the set of run cards that share the same `Import ID`; a suite run is simply the set of run cards that share the same `Test Suite Run` tag.

## `test-case-runs.json`

Each object represents one actionable run card.

### Identity

- `importRunId`
  Stable primary key for the run card. Format: `caseImportId::suiteRunKey`. Used as the import-state key and for dedup.

- `caseImportId`
  Stable identifier of the underlying case. Shared by every run card for the same case. Imported into the `Import ID` property so runs of one case can be grouped.

- `suiteRunKey`
  Slug form of the suite-run name. Used only to build `importRunId`; not sent to Notion.

- `sourceRowNumber`
  Original CSV row number.

### Suite-run tag

- `suiteRunTag`
  Human-readable suite-run name (e.g. `6.3`). Imported into the `Test Suite Run` select property. This is the closed selectable list that replaces the old suite-run relation.

  Two normalizations apply during preparation:

  - The `BetaInternal` qualifier is stripped from suite-run names (e.g. `5.4 BetaInternal` becomes `5.4`).
  - Only suite runs at or after version **5.5** are imported. Older suite-run columns are dropped before any rows are read, so no run cards are produced for them. The cutoff is `MIN_SUITE_RUN` in `prepare-import.mjs`; the leading `major.minor` is compared as integers.

### Folded case metadata

- `caseSummary`
  The case title. Imported into `Case Summary` (the run-card title itself is `suiteRunName/caseReference`).

- `legacyNumber`
  Spreadsheet legacy identifier when present.

- `dokimionId`
  External test identifier.

- `priority`
  Normalized priority label. Allowed values normalize to `1`, `2`, `3`, `Ignore`, or `Duplicate`.

- `pastIssues`
  Historical issue references, often `BL-` IDs. Rendered with hyperlinks.

- `estTimeMin`
  Estimated time in minutes, or `null`.

- `areas`
  Functional areas the case belongs to, derived from area-heading context rows and `area-mapping.json`. Imported into the `Areas` multi-select.

- `originalDescription`
  The raw spreadsheet description before any area instruction was prepended. Imported into `Original Description`.

- `description` / `caseSnapshot`
  The prepared case description text, with any area-level instruction prepended. Used as the page-body fallback when no checklist steps were derived. (`caseSnapshot` is the body source; `description` is kept alongside it for clarity.)

- `stepDescription`
  A compact one-line summary of the case's actions. Imported into `Step Description`.

- `checklistSteps`
  The parsed, ordered action steps for the case. Rendered as checkable to-do items in the page body.

- `stepNotes`
  Verification/note lines parsed out of the description. Rendered as to-do items after the steps.

- `bodyChecklistItems`
  The deduped union of `checklistSteps` and `stepNotes`. When present, this is the preferred source for the page-body checklist.

### Execution fields

- `title`
  Card title shown in Notion, derived as `suiteRunName/caseReference`.

- `assignee`
  The tester, mapped to a closed set of canonical names (`Andrew`, `Bharani`, `Hatton`, `Jeffrey`, `JohnT`, `Suzanne`, `Steve`, `Noel`, `Marlon`, `Heather`, `Colin`, `Gordon`); `SteveMc` maps to `Steve`. Any cell that does not match one of these — a skipped run, `Future`, a review comment typed into the cell, or an unknown name — becomes `""`. The raw value is always preserved in `executionEntries`. Imported into the `Assignee` select.

- `skipped`
  `true` when the run's assignee cell starts with `skip` (e.g. `skip`, `SKIP (AP)`, `Skip: fix in 5.5`), meaning the test was deliberately not run in that suite run. Imported into the `Skipped` checkbox.

- `testedOn`
  Normalized ISO-like date string when available.

- `buildTested`
  Build information from the execution columns.

- `issueLinks`
  Issue references aggregated across the grouped execution entries for the suite run. Rendered with hyperlinks.

- `ok`
  Normalized raw OK flag (`__YES__` / `__NO__` / `""`). Imported into the `OK` checkbox.

- `historicalImport`
  Marks rows brought in from the historical spreadsheet. Imported into the `Historical Import` checkbox.

- `executionEntries`
  Raw grouped execution rows that fed this run card. Rendered into the page body as detail bullets.

## `suite-run-tags.json`

Each object documents one `Test Suite Run` tag value:

- `tag` — display name (the select option value)
- `key` — slug used inside `importRunId`
- `runOrder` — relative order discovered from the spreadsheet column position

This file is a reference/sanity artifact. The importer does not read it; Notion select options are created on demand as run cards are written.

## OK Derivation

The `OK` checkbox is set from `record.ok`:

- `OK = true` when `ok === '__YES__'` **and the run was not skipped**
- `OK = false` otherwise

A skipped run is never marked OK, even if the source `OK?` cell said yes. This is intentionally a simple boolean, not a multi-state workflow status; `Skipped` is a separate checkbox.

## Live Notion Database

The target database lives under `parentPageId` in `notion-config.json`. On a clean slate, leave `databases.testCaseRuns` empty and the importer creates the `Test Case Runs` database with the schema below, then records its id in `notion-state.json`.

When the database already exists, the importer reconciles its live schema enough to support the current import contract. This document describes the live property contract the importer expects today.

### Notion Mapping: `Test Case Runs`

Database title property name:

- `Test Case Run`

Properties written by `buildCaseRunProperties()`:

- `Test Case Run` -> Notion `title`
  From `record.title`

- `Import Run ID` -> Notion `rich_text`
  From `record.importRunId`

- `Import ID` -> Notion `rich_text`
  From `record.caseImportId`

- `Test Suite Run` -> Notion `select`
  From `record.suiteRunTag`. The closed, selectable suite-run list. Option names cannot contain commas (commas are replaced with spaces).

- `Case Summary` -> Notion `rich_text`
  From `record.caseSummary`

- `Legacy Number` -> Notion `rich_text`
  From `record.legacyNumber`

- `Dokimion ID` -> Notion `rich_text`
  From `record.dokimionId`

- `Past Issues` -> Notion `rich_text`
  From `record.pastIssues`, with `BL-1234` style references converted to hyperlinks

- `Priority` -> Notion `select`
  Optional, from `record.priority`

- `Est. Time (min)` -> Notion `number`
  Optional, from `record.estTimeMin`

- `Step Description` -> Notion `rich_text`
  From `record.stepDescription`

- `Original Description` -> Notion `rich_text`
  From `record.originalDescription`

- `Areas` -> Notion `multi_select`
  From `record.areas`

- `Assignee` -> Notion `select`
  From `record.assignee` (a canonical name) when set. The select offers the fixed assignee set (`ASSIGNEE_OPTIONS`); blank assignees leave the property empty.

- `Build Tested` -> Notion `rich_text`
  From `record.buildTested`

- `Issue Links` -> Notion `rich_text`
  From `record.issueLinks`, with `BL-1234` style references converted to hyperlinks

- `OK` -> Notion `checkbox`
  From `record.ok`

- `Skipped` -> Notion `checkbox`
  From `record.skipped`

- `Historical Import` -> Notion `checkbox`
  From `record.historicalImport`

- `Source Row Number` -> Notion `number`
  From `record.sourceRowNumber`

- `Tested On` -> Notion `date`
  Optional, from `record.testedOn`

Run-card page body behavior:

- a `Test Steps` heading followed by the test case checklist as `to_do` blocks (from `bodyChecklistItems`, or `checklistSteps` + `stepNotes`); when no steps were derived it falls back to the case description as paragraph blocks
- an `Imported Execution Details` heading followed by one bullet per execution entry
- by default the body is only written when empty; set `IMPORT_REPLACE_BODY=1` to delete and rewrite it

## Live Schema Reconciliation

`import-to-notion.mjs` can reconcile the live `Test Case Runs` schema before import (enabled by default; set `IMPORT_RECONCILE_SCHEMA=0` to skip).

Current behavior:

- drops obsolete properties if present: the `Test Case` relation and the `Active` checkbox (see `OBSOLETE_RUN_PROPERTIES`)
- drops `Test Suite Run` if it still exists as a `relation`, then re-adds it as a `select`
- adds any of the required run properties that are missing (see `REQUIRED_RUN_PROPERTIES` in the importer)

The old `Test Cases` and `Test Suite Runs` databases are never modified.

## Import State And Identity

The importer is rerunnable. Run cards are keyed by `importRunId` in `notion-state.json`.

If a page ID is already present in `notion-state.json`, the importer updates that page. If not, and lookup is allowed, it queries the live database by `Import Run ID`.

## Page Body Replacement Behavior

- default mode: only append blocks if the page body is empty
- replacement mode (`IMPORT_REPLACE_BODY=1`): delete existing child blocks and rewrite the body

Use replacement mode when the prepared body format changes and old content should be discarded.

## Useful Environment Variables

### Preparation

- `IMPORT_LIMIT_CASES`
  Limit the number of importable cases processed (default `10`; set `0` for no limit).

- `IMPORT_CASE_OFFSET`
  Skip this many importable cases before starting (default `0`). Useful for processing a later slice of the spreadsheet.

### Import

- `IMPORT_ALLOW_LOOKUP`
  Enable live lookup by `Import Run ID` when state is missing (default on; set `0` to disable).

- `IMPORT_RECONCILE_SCHEMA`
  Reconcile the live `Test Case Runs` schema (default on; set `0` to disable).

- `IMPORT_RESET_EXISTING`
  Archive existing pages in the `Test Case Runs` database before import.

- `IMPORT_REPLACE_BODY`
  Replace existing page-body content instead of leaving it alone.

- `IMPORT_RETRY_COUNT`, `IMPORT_RETRY_DELAY_MS`, `IMPORT_REQUEST_TIMEOUT_MS`
  Notion API retry/backoff/timeout tuning.

## Auxiliary Output Files

- `prepare-summary.json`
  Compact summary of the preparation pass: source CSV path, output dir, case limit, slot count, case count, suite-run tag count, run count, date-warning count.

- `date-warnings.json`
  Rows where date normalization was not clean. Review if imported `Tested On` coverage looks wrong.

- `notion-state.json`
  Persistent mapping from `importRunId` to Notion page ID. Operational state, not a source document.

- `notion-import-failures.json`
  Operational error log. An empty `caseRuns` array means the latest import pass completed cleanly.

## Recommended Mental Model For Future Work

When changing the import, use this order of concern:

1. Improve preparation quality in `prepare-import.mjs`.
2. Keep `import-to-notion.mjs` focused on mapping and transport into the single database.
3. Treat the `Test Suite Run` select tag as the only suite-run modeling.
4. Verify against the live Notion schema, not just this document.

## Practical Scratch-Start Checklist

If you had no session memory and needed to resume this work from scratch:

- `prepare-import.mjs` creates `test-case-runs.json` plus `suite-run-tags.json`, a summary, and a warnings file
- `import-to-notion.mjs` uploads `test-case-runs.json` into the single `Test Case Runs` database defined in `notion-config.json`
- there is only one Notion database now
- suite-run membership is the `Test Suite Run` select tag, not a relation
- each run card is the merge of the test case definition and one run: case metadata is folded on as properties, and the parsed checklist steps/notes are the page-body to-do list
- `prepare-import.mjs` still reads `area-mapping.json`, `title-mapping.json`, and `step-overrides.json` to interpret the spreadsheet
- `OK` is a simple checkbox derived from the spreadsheet `OK?` column
- the live Notion schema matters more than older design assumptions

That is the import contract.
